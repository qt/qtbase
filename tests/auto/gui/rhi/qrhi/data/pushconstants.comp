#version 440

layout(local_size_x = 16) in;

layout(std430, binding = 0) buffer Out {
    uint data[];
} outBuf;

layout(push_constant) uniform PC {
    uint base;
    uint stride;
} pc;

void main()
{
    uint i = gl_GlobalInvocationID.x;
    outBuf.data[i] = pc.base + i * pc.stride;
}
