plugins {
    alias(libs.plugins.android.application)
}

dependencies {
    @GRADLE_DEPENDENCIES@
}

android {
    namespace = '@PACKAGE_NAME@'
    compileSdk {
        version = release(@ANDROID_COMPILE_SDK_RELEASE@) {
            minorApiLevel = @ANDROID_COMPILE_SDK_MINOR@
        }
    }
    buildToolsVersion = '@ANDROID_BUILD_TOOLS_VERSION@'
    ndkVersion = '@ANDROID_NDK_REVISION@'

    defaultConfig {
        @DEFAULT_CONFIG_VALUES@
        applicationId = "@APP_PACKAGE_NAME@"
        versionCode = @APP_VERSION_CODE@
        versionName = "@APP_VERSION_NAME@"
        manifestPlaceholders = [
            appName: "@APP_NAME@",
            appLibName: "@APP_LIB_NAME@",
            appArguments: "@APP_ARGUMENTS@"
        ]
    }

    sourceSets {
        main {
            @SOURCE_SETS@
        }
    }

    tasks.withType(JavaCompile).configureEach {
        options.compilerArgs += ['-Xlint:all']
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    androidResources {
        noCompress += ['rcc'] // Do not compress Qt binary resources file
    }

    @ANDROID_DEPLOYMENT_EXTRAS@
}
